# vim: ft=sh

# some common ls aliases
alias ll='ls -l'
alias la='ls -A'
alias l='ls -CF'

# git aliases
alias gst='git status'
alias gc='git commit'
alias gd='git diff'
alias ga='git add'

alias ra='$EDITOR ~/etc/bash/aliases; source ~/etc/bash/aliases'
alias pc='perlcritic --quiet .'
